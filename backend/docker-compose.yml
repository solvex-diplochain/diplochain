version: '3.9'

services:
  mongo:
    image: mongo:6.0
    container_name: diplochain-mongo
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
    name: diplochain-mongo-data
